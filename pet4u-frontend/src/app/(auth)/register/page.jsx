import RegisterForm from '@/app/_component/auth/RegisterForm';

export const metadata = {
  title: 'Register - PET4U',
  description: 'Create a new PET4U account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
